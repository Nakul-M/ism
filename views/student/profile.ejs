

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-10">
      <div class="card shadow-lg border-0 rounded-4 p-4">
        <div class="row g-4 align-items-center">
          <!-- Left: Student Photo -->
          <div class="col-md-4 text-center">
            <img src="<%='https://passport-photo.online/_optimized/prepare2.0498e1e2-opt-1920.WEBP'%>" alt="Student Photo" class="img-thumbnail rounded-circle shadow" style="width: 200px; height: 200px; object-fit: cover;">
            <h4 class="mt-3 fw-bold text-primary"><%= student.name %></h4>
            <span class="badge bg-secondary fs-6"><%= student.class %> - <%= student.section %></span>
          </div>

          <!-- Right: Student Info -->
          <div class="col-md-8">
            <h3 class="mb-3 text-dark border-bottom pb-2">Student Profile</h3>
            <div class="row g-3">
              <div class="col-sm-6">
                <p><i class="bi bi-hash"></i> <strong>Roll No:</strong> <%= student.rollNo %></p>
                <p><i class="bi bi-calendar-date"></i> <strong>Date of Birth:</strong> <%= student.dateOfBirth %></p>
                <p><i class="bi bi-geo-alt"></i> <strong>Address:</strong> <%= student.address || "N/A" %></p>
              </div>
              <div class="col-sm-6">
                <p><i class="bi bi-person"></i> <strong>Parent Name:</strong> <%= student.parentName || "N/A" %></p>
                <p><i class="bi bi-telephone"></i> <strong>Parent Contact:</strong> <%= student.parentContact || "N/A" %></p>
                <p><i class="bi bi-phone"></i> <strong>Student Contact:</strong> <%= student.contact %></p>
              </div>
            </div>

            <div class="mt-4 d-flex justify-content-end">
              <a href="/student/<%= student._id %>" class="btn btn-outline-primary me-2">
                <i class="bi bi-speedometer2"></i> Back to Dashboard
              </a>
            
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
